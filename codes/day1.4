import javax.script.ScriptEngineManager;
import javax.script.ScriptEngine;
import javax.script.ScriptException;

public class EvaluateExpression {
    public static double evaluate(String expression) throws ScriptException {
        ScriptEngineManager manager = new ScriptEngineManager();
        ScriptEngine engine = manager.getEngineByName("JavaScript");
        return (double) engine.eval(expression);
    }

    public static void main(String[] args) {
        String exp1 = "10 + 2 * 6";
        String exp2 = "100 * (2 + 12) / 14";
        
        try {
            System.out.println("Result of '" + exp1 + "' = " + evaluate(exp1));
            System.out.println("Result of '" + exp2 + "' = " + evaluate(exp2));
        } catch (ScriptException e) {
            System.out.println("Error evaluating expression: " + e.getM
